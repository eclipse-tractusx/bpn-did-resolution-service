apiVersion: v1
kind: Service
metadata:
  name: {{ include "bdrs-server.fullname" . }}
  labels:
    {{- include "bdrs-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bdrs-server.selectorLabels" . | nindent 4 }}
